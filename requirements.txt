# pessimistic operator for local tdd installs (pip install -e .)
py-pve-cloud>=3.3.1,<3.4.0
kubernetes==34.1.0
requests==2.32.5
zstandard==0.25.0
# todo: this should probably be moved to top level py-pve-cloud
# or removed from controller for pickeling
tinydb==4.8.2
python-socketio[asyncio_client]==5.16.4
aiohttp==3.14.3
